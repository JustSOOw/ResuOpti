/**
 * 业务通用组件导出文件
 * 统一导出业务组件，方便其他模块引用
 */

import ResumeCard from './ResumeCard.vue'
import FileUpload from './FileUpload.vue'

export { ResumeCard, FileUpload }

export default {
  ResumeCard,
  FileUpload
}
